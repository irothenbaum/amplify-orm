/**
 * @param {{{inputType}}} input
 * @returns {Promise<*>}
 */
async {{functionName}}(input) {
  return this.{{internalFunction}}(
     {{functionName}},
     input,
     this.fragmentName,
  )
}

/**
 * @param {{{inputType}}} input
 * @returns {Promise<*>}
 */
static async {{functionName}}(input) {
  return new {{collectionName}}().{{functionName}}(input)
}